---
// Header.astro — Navbar da biblioteca Astroteca
//
// Comportamentos obrigatórios (vanilla JS, sem React):
//   - Esconde ao rolar para baixo, reaparece ao rolar para cima
//   - Link ativo destacado conforme a seção visível (IntersectionObserver)
//   - Menu mobile fullscreen, fecha ao clicar em link ou no backdrop
//
// O <header> tem id="site-header" (usado pelo CSS de scroll).
// Os links devem apontar para seções reais (#id) — nunca para "#".
//
// Tokens: estilos escopados usam var(--t-*) do tokens.css do projeto cliente
// (--t-primary, --t-surface, --t-border, --t-text-main, --t-font-serif, etc.).
// Valores sem token (altura 4rem, max-width 1200px) são literais.

interface NavLink {
  label: string;
  href: string;
}

interface Props {
  logo: string;
  logoHref?: string;
  links?: NavLink[];
  ctaLabel?: string;
  ctaHref?: string;
}

const {
  logo,
  logoHref = "/",
  links = [],
  ctaLabel,
  ctaHref = "#contato",
} = Astro.props;
---

<header id="site-header" data-site-header>
  <div class="header__inner">
    <a href={logoHref} class="header__logo">{logo}</a>

    {links.length > 0 && (
      <nav class="header__nav" aria-label="Navegação principal">
        {links.map((link) => (
          <a href={link.href} class="header__link" data-nav-link={link.href}>
            {link.label}
          </a>
        ))}
      </nav>
    )}

    <div class="header__actions">
      {ctaLabel && (
        <a href={ctaHref} class="header__cta">{ctaLabel}</a>
      )}

      {links.length > 0 && (
        <button
          type="button"
          class="header__burger"
          aria-label="Abrir menu"
          aria-expanded="false"
          aria-controls="mobile-menu"
          data-menu-toggle
        >
          <span></span><span></span><span></span>
        </button>
      )}
    </div>
  </div>

  {links.length > 0 && (
    <div id="mobile-menu" class="header__mobile" data-mobile-menu hidden>
      <button type="button" class="header__mobile-backdrop" aria-label="Fechar menu" data-menu-close></button>
      <nav class="header__mobile-nav" aria-label="Navegação mobile">
        {links.map((link) => (
          <a href={link.href} class="header__mobile-link" data-menu-close>
            {link.label}
          </a>
        ))}
        {ctaLabel && (
          <a href={ctaHref} class="header__cta header__cta--mobile" data-menu-close>{ctaLabel}</a>
        )}
      </nav>
    </div>
  )}
</header>

<style>
  #site-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 50;
    height: 4rem;
    background-color: var(--t-surface);
    border-bottom: 1px solid var(--t-border);
    transform: translateY(0);
    transition: transform 300ms ease-out;
  }
  #site-header[data-hidden] {
    transform: translateY(-100%);
  }

  .header__inner {
    max-width: 1200px;
    height: 100%;
    margin: 0 auto;
    padding: 0 clamp(1rem, 5vw, 2rem);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 2rem;
  }

  .header__logo {
    font-family: var(--t-font-serif);
    font-weight: 600;
    font-size: 1.25rem;
    color: var(--t-text-main);
    flex-shrink: 0;
  }

  .header__nav {
    display: none;
    align-items: center;
    gap: 1.75rem;
  }
  .header__link {
    font-family: var(--t-font-sans);
    font-size: 0.9rem;
    color: var(--t-text-soft);
    transition: color 150ms ease-out;
    position: relative;
  }
  .header__link:hover { color: var(--t-text-main); }
  .header__link[data-active] { color: var(--t-primary); }
  .header__link[data-active]::after {
    content: "";
    position: absolute;
    left: 0;
    bottom: -4px;
    width: 100%;
    height: 2px;
    background-color: var(--t-primary);
  }

  .header__actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .header__cta {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background-color: var(--t-primary);
    color: white;
    font-family: var(--t-font-sans);
    font-weight: 500;
    font-size: 0.9rem;
    padding: 0.625rem 1.5rem;
    border-radius: 0.375rem;
    transition: background-color 150ms ease-out;
  }
  .header__cta:hover { background-color: var(--t-primary-dark); }

  .header__burger {
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 5px;
    width: 2.5rem;
    height: 2.5rem;
    padding: 0.5rem;
    background: none;
    border: none;
    cursor: pointer;
  }
  .header__burger span {
    display: block;
    width: 1.25rem;
    height: 2px;
    background-color: var(--t-text-main);
    transition: transform 300ms ease-out, opacity 150ms ease-out;
  }
  .header__burger[aria-expanded="true"] span:nth-child(1) { transform: translateY(7px) rotate(45deg); }
  .header__burger[aria-expanded="true"] span:nth-child(2) { opacity: 0; }
  .header__burger[aria-expanded="true"] span:nth-child(3) { transform: translateY(-7px) rotate(-45deg); }

  .header__mobile {
    position: fixed;
    inset: 0;
    z-index: 49;
  }
  .header__mobile[hidden] { display: none; }
  .header__mobile-backdrop {
    position: absolute;
    inset: 0;
    background-color: oklch(0% 0 0 / 0.4);
    border: none;
    cursor: pointer;
  }
  .header__mobile-nav {
    position: absolute;
    top: 0;
    right: 0;
    height: 100%;
    width: min(80vw, 320px);
    background-color: var(--t-surface);
    padding: calc(4rem + 1.5rem) 1.5rem 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.15);
  }
  .header__mobile-link {
    font-family: var(--t-font-sans);
    font-size: 1.125rem;
    color: var(--t-text-soft);
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--t-border);
  }
  .header__cta--mobile {
    margin-top: 1rem;
    width: 100%;
  }

  @media (min-width: 768px) {
    .header__nav { display: flex; }
    .header__burger { display: none; }
  }
</style>

<script>
  // ── Scroll direction: esconde ao descer, mostra ao subir ──
  const header = document.querySelector<HTMLElement>("[data-site-header]");
  if (header) {
    let lastY = window.scrollY;
    window.addEventListener(
      "scroll",
      () => {
        const y = window.scrollY;
        // Sempre visível perto do topo
        if (y < 80) {
          header.removeAttribute("data-hidden");
        } else if (y > lastY) {
          header.setAttribute("data-hidden", "");
        } else {
          header.removeAttribute("data-hidden");
        }
        lastY = y;
      },
      { passive: true },
    );
  }

  // ── Link ativo conforme seção visível ──
  const navLinks = Array.from(
    document.querySelectorAll<HTMLAnchorElement>("[data-nav-link]"),
  );
  const sections = navLinks
    .map((a) => {
      const id = a.getAttribute("data-nav-link") ?? "";
      return id.startsWith("#") ? document.querySelector(id) : null;
    })
    .filter((el): el is Element => el !== null);

  if (sections.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (!entry.isIntersecting) continue;
          const id = `#${entry.target.id}`;
          for (const link of navLinks) {
            link.toggleAttribute(
              "data-active",
              link.getAttribute("data-nav-link") === id,
            );
          }
        }
      },
      { rootMargin: "-40% 0px -55% 0px" },
    );
    for (const section of sections) observer.observe(section);
  }

  // ── Menu mobile ──
  const toggle = document.querySelector<HTMLButtonElement>("[data-menu-toggle]");
  const menu = document.querySelector<HTMLElement>("[data-mobile-menu]");

  function closeMenu() {
    if (!toggle || !menu) return;
    toggle.setAttribute("aria-expanded", "false");
    menu.hidden = true;
    document.body.style.overflow = "";
  }
  function openMenu() {
    if (!toggle || !menu) return;
    toggle.setAttribute("aria-expanded", "true");
    menu.hidden = false;
    document.body.style.overflow = "hidden";
  }

  if (toggle && menu) {
    toggle.addEventListener("click", () => {
      if (menu.hidden) openMenu();
      else closeMenu();
    });
    for (const el of menu.querySelectorAll("[data-menu-close]")) {
      el.addEventListener("click", closeMenu);
    }
    document.addEventListener("keydown", (e) => {
      if (e.key === "Escape") closeMenu();
    });
  }
</script>
